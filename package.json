{
    "name": "frontend",
    "version": "0.1.0",
    "description": "Neccesary file for client CI",
    "main": "server/app.js",
    "scripts": {
      "postinstall": "npm install --prefix client && npm install --only=dev --prefix client && npm run build --prefix client"
    },
    "engines": {
      "npm": "6.x",
      "node": "14.x"
    },
    "devDependencies": {
      "concurrently": "^7.4.0",
      "nodemon": "^2.0.19"
    }
  }
  